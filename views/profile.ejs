<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen text-white bg-zinc-900 p-10">
      <div class="w-full flex justify-end">
        <a
          href="/logout"
          class="px-4 py-2 bg-red-700 block rounded-md w-20 text-center mb-1"
          >Logout</a>
      </div>
      <div class="flex justify-center -mt-10 mb-1">
  <div class="w-20 h-20 rounded-full overflow-hidden border-2 border-white">
    <img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic%>" alt="icon"/>
  </div>
</div>
      <h3 class="text-center text-4xl mb-2">
        <span class="text-pink-500">Hello,</span><%= user.name %>👋
      </h3>
      <h5 class="text-center text-xl">You can create a new post</h5>

      <form
        method="post"
        action="/post"
        class="flex flex-col items-center mt-6"
      >
        <textarea
          placeholder="Write your post..."
          class="p-3 w-full max-w-2xl h-56 resize-none bg-transparent border-2 border-white rounded-md"
          name="content"
        ></textarea>
        <input
          class="px-4 w-50 py-2 bg-green-700 block rounded-md mt-2"
          type="submit"
          value="Create New Post 🕊️"
        />
      </form>
      <div class="posts mt-2">
        <h3 class="text-blue-700 text-xl">your posts</h3>
        <div class="postcontainer w-full mt-4 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
          <%user.posts.reverse().forEach(function(post){ %>
            <div class="post p-3 mb-2 border-2 border-zinc-700 bg-zinc-800">
            <!-- <h4 class="text-lg font-bold">Post Title</h4> -->
            <h3 class="text-blue-500 mb-2">@<%= user.username %></h3>
            <p class="text-sm tracking-tight mt-1 px-1">
              <%= post.content %>
            </p>
            <small class=" px-1 mt-2 inline-block"><%= post.likes.length %> likes</small>
            <div class="btns flex  gap-2">
              <a href="/like/<%= post._id %>" class="text-pink-700 text-lg"><%=post.likes.indexOf(user._id)===-1?"🤍":"❤️" %></a>
              <a href="/edit/<%= post._id %>" class="text-zinc-400 text-lg">edit</a>
              <a href="/delete/<%= post._id %>" class="text-red-500 text-lg ml-auto">delete</a>
            </div>
          </div>
          <% }) %>
        </div>
        
      </div>
    </div>
  </body>
</html>
