<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen text-white bg-zinc-900 p-10">
      <div class="w-full flex justify-end">
        <a
          href="/logout"
          class="px-4 py-2 bg-red-700 block rounded-md w-20 text-center mb-1"
          >Logout</a
        >
      </div>

      <h5 class="text-center text-3xl">Edit your post</h5>

      <form
        method="post"
        action="/update/<%= post._id %>"
        class="flex flex-col items-center mt-6"
      >
        <textarea
          placeholder="Write your post..."
          class="p-3 w-full max-w-2xl h-56 resize-none bg-transparent border-2 border-white rounded-md"
          name="content"
        >
        <%= post.content %></textarea
        >
        <input
          class="px-2 w-40 py-2 bg-yellow-500 text-black text-lg block rounded-md mt-2"
          type="submit"
          value=" Update Post"
        />
      </form>
    </div>
  </body>
</html>
